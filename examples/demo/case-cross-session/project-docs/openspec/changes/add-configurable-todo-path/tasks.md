# Tasks

- [x] T1 定义 `--file` / `TODO_FILE` / 默认值优先级契约
- [x] T2 解析 `--file` 参数
- [x] T3 支持 `TODO_FILE` 环境变量回退
- [x] T4 保证默认 `.todo.txt` 行为不回归
- [x] T5 补聚焦测试（含未知 id 错误路径）
- [x] T6 跑 verify、写 handover、收口与 learn

> 本文件同时被 `.zimaflow-state.yaml` 的 `openspec.tasks_path` 引用。
