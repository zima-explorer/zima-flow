# todo list 状态筛选 · Spec Compliance Report

> 类型：spec-compliance-check 审查报告
> 依据模板：`skills/spec-compliance-check.md`
> change：list-status-filter
> 触发条件：全部 task（T1–T5）完成后的全量审查
> 审查时间：2026-07-29

## Spec Compliance Report

**变更**：list-status-filter
**审查时间**：2026-07-29

### 场景覆盖

对照 [需求 brief](../Requirements/2026-07-29-list-status-filter-brief.md) 的 Given/When/Then：

- ✅ 场景 1：`list --status pending` 显示未完成、不含已完成 — 已实现（`cmd_list` 分区开关），已测试（检查 1 正向 + 检查 2 负向）
- ✅ 场景 2：`list --status done` 显示已完成、不含未完成 — 已实现，已测试（检查 3，正负向合并断言）
- ✅ 场景 3：不传 `--status` 与 `--status all` 输出一致 — 已实现（默认值 `all`），已测试（检查 4，逐字符比对两次输出）
- ✅ 场景 4：`--status bogus` 报清晰错误并以非 0 退出 — 已实现（参数解析的取值白名单），已测试（检查 5）
- ✅ 场景 5：`--status` 与 `--file` 可组合 — 已实现（两者在同一扫描循环中解析，互不依赖），已测试（检查 6）

> 审查备注：场景 1、2 的负向部分不是补充项而是主证据。若只断言「目标项存在」，即使 `--status` 被完全忽略、两个分区照常全渲染，断言仍会通过——这类假绿灯正是本案例要防的。

### 架构一致性 / 范围一致性

轻量模式下没有 design.md，改为对照 brief 的「范围」与「不做什么」核对实现：

- ✅ 只在读取侧加开关：改动集中在参数解析循环与 `cmd_list` 的渲染分区，未触碰 `cmd_add` / `cmd_done`
- ✅ 参数位置无关：`--status` 与 `--file` 共用同一个前置扫描循环，不依赖出现位置
- ✅ 默认值为 `all`：默认行为不回归的约束落在实现的初始赋值上，而不是靠调用方传参保证

### 排除范围

对照 brief 的「不做什么」：

- ✅ 未触碰排除范围：持久化格式仍为 `id|done|title`，未新增字段、未做迁移；未实现 JSON 输出；未实现日期过滤；`add` / `done` 的行为与输出格式未改动

### 破坏性变更（B4）

- ✅ 无破坏性变更：未删除代码（≥5 行）、未改公共接口既有签名、未改 schema / 持久化格式、未涉及权限鉴权、未改数据写入路径（本轮新增的是纯读命令的一个可选参数）。唯一的接口变化是新增可选参数 `--status`，不传时行为与改动前完全一致，已由检查 4 锁定。

### 沿用抽象（B5）

- ✅ 已沿用现有抽象：`cmd_list` 本来就按 done 位分两区渲染，判定逻辑（`[ "$done" = "0" ]` / `[ "$done" = "1" ]`）已经存在。本轮**沿用这套既有判定，只在外层加一个是否渲染该分区的开关**，没有另写一套状态过滤器。
- 这正是本次需求的性质：缺的不是判定能力，是让用户选择的入口。动手前先确认底层判定是否已存在，避免了重复实现。

### 验证证据匹配度（Step 4.7）

"测试通过"不等于"验证充分"——不同变更类型该看的证据不同。本次实现命中 **1 类**：

- **命中类型**：新增 / 扩展 CLI 参数或入口行为。期望证据：**契约测试 + 兼容性检查**。
- ✅ **契约测试**：检查 1（正向筛选）、检查 2 与检查 3（负向排除）、检查 5（非法取值的异常路径）。
- ✅ **兼容性检查**：检查 4（不传 `--status` 与 `--status all` 输出一致，默认行为不回归）、检查 6（`--status` 与上一个案例交付的 `--file` 组合可用）。检查 4 是**因为矩阵把「兼容性检查」与「契约测试」并列为该类型的期望证据才单独立的**，不是顺手多写的。
- **证据位置**：`app/test-todo.sh`，输出 `verify passed: 6/6 checks`，命令可复跑。
- **其余类型不涉及**：改数据库 / 持久化格式、改 MQ producer-consumer、改状态机 / 流程状态、改权限 / 鉴权、重构 —— 本次改动未触及持久化格式、消息、状态流转、鉴权，也未做重构，因此这五类**不涉及**（不是"未覆盖"，两者含义不同）。

> 这是 soft check：只核对证据是否与变更类型匹配，不阻断流程、不自动补测试。矩阵管"该看哪类证据"，不管"证据本身准不准"。

### 结论

- [x] 全部通过 → 可进入 close
- [ ] 有未覆盖场景 → 需要补实现
- [ ] 有设计偏离 → 需要决策：更新规范还是修改实现
- [ ] 超出范围 → 必须回退超范围的改动
- [ ] 有破坏性变更未评估 → 补引用面排查 + 影响说明后由用户决策
- [ ] 有未沿用的现有抽象 → 由用户决定是否重构沿用

## 证据指针

- 实现：[`app/todo.sh`](../../../app/todo.sh)
- 测试脚本：[`app/test-todo.sh`](../../../app/test-todo.sh)（`verify passed: 6/6 checks`）
- 规范来源：[需求 brief](../Requirements/2026-07-29-list-status-filter-brief.md)（Given/When/Then）
- 任务台账：[轻量任务台账](../Tasks/2026-07-29-list-status-filter-tasks.md)
