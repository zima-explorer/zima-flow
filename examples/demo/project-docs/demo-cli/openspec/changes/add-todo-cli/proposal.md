# 新增 Todo CLI

## 为什么

demo 项目需要一个足够小的功能，用来演示完整 zimaflow 路径，同时不依赖外部服务或凭证。

## 改动内容

- 增加包含 `add`、`list` 和 `done` 的命令行接口。
- 使用本地 JSON 文件保存任务。
- 为主流程增加测试。

## 不做什么

- 不跨机器同步数据。
- 不做多用户权限。
- 不做 Web 或移动端 UI。
- 不做通知。
