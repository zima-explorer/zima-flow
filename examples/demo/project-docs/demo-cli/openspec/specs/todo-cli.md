# Todo CLI 规范

## 场景：添加并查看任务

假设 todo 文件为空，
当用户添加 `Write README`，
那么任务列表会显示 `Write README` 为待办。

## 场景：完成任务

假设 todo 文件中有任务 id `1`，
当用户将任务 `1` 标记为完成，
那么任务列表会显示任务 `1` 已完成。

## 场景：未知任务 id

假设 todo 文件中没有任务 id `99`，
当用户将任务 `99` 标记为完成，
那么命令会报告清晰的用户错误。
