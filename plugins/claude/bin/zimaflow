#!/usr/bin/env bash
set -euo pipefail

PLUGIN_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
ZIMAFLOW_HOME="$PLUGIN_ROOT/runtime/zimaflow"
export ZIMAFLOW_HOME
exec "$PLUGIN_ROOT/runtime/zimaflow/bin/zimaflow" "$@"
